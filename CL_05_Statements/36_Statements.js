// A statement is a single instruction that the program executes.
// It tells JavaScript to do something

let a = 10;          // variable statement
console.log(a);      // print statement
a = a + 5;           // assignment statement

/* Each line is a statement.
Think: One instruction = one statement */

let age = 20;

if (age > 18) {
    console.log("Yes, Goa");
} else {
    console.log("Go home!");
}

// Statement - switch on light